[defaults]
inventory = inventory.ini
host_key_checking = false

[privilege_escalation]
# Remove (or set false) if managed nodes grant the automation user
# passwordless sudo; otherwise run plays with -K.
become_ask_pass = true
